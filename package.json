{
  "name": "hitrate-firestore-rules",
  "private": true,
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-hitrate-rules --only firestore \"node --test FirestoreRulesTests/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
